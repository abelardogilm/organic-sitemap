require 'spec_helper'

describe OrganicSitemap do
  it 'has a version number' do
    expect(OrganicSitemap::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
